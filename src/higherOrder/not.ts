/**
 * Returns the logical negation of a value.
 *
 * @param x - Boolean value.
 * @returns `true` if `x` is `false`, `false` if `x` is `true`.
 *
 * @example
 * not(true)  // false
 * not(false) // true
 */
export function not(x: boolean): boolean {
  return !x;
}